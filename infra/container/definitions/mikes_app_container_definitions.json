[
  {
    "name": "${NAME}",
    "image": "644237782704.dkr.ecr.us-east-2.amazonaws.com/mikes_pedido:latest",
    "memory": 512,
    "essential": true,
    "portMappings": [
      {
        "containerPort": 8080,
        "hostPort": 8080
      }
    ],
    "environment": [
      {
        "name": "DB_HOST",
        "value": "${DB_HOST}"
      },
      {
        "name": "DB_PORT",
        "value": "${DB_PORT}"
      },
      {
        "name": "DB_NAME",
        "value": "${DB_NAME}"
      },
      {
        "name": "DB_USER",
        "value": "${DB_USER}"
      },
      {
        "name": "DB_PASSWORD",
        "value": "${DB_PASSWORD}"
      },
      {
        "name": "SNS_ORDER_RECEIVED_NAME",
        "value": "${SNS_ORDER_RECEIVED_NAME}"
      },
      {
        "name": "SNS_ORDER_CONFIRMED_NAME",
        "value": "${SNS_ORDER_CONFIRMED_NAME}"
      },
      {
        "name": "AWS_REGION",
        "value": "${REGION}"
      }
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${LOG_GROUP_NAME}",
        "awslogs-region": "us-east-2",
        "awslogs-stream-prefix": "ecs"
      }
    }
  }
]